{
  "backend": "mxmaca++",
  "status": "not_implemented_in_this_environment",
  "source_policy": "pin_public_repo_or_sdk_version_and_license_before_use",
  "required_inputs": ["case_id", "shape", "dtype", "seed", "parameters"],
  "required_outputs": ["status", "correctness", "timing", "environment", "source_ref"],
  "must_record": ["mxmaca_version", "mxcc_version", "compiler_command", "device_string", "synchronization_api", "source_commit"],
  "correctness_reference": "pytorch",
  "timing_unit": "ms",
  "speedup_rule": "compare only with identical environment_fingerprint and case_id",
  "notes": [
    "Resolve actual MXMACA++ headers, launch syntax, and synchronization on the provided C500 host.",
    "Do not label a host-side compile or placeholder as a completed backend result."
  ]
}
